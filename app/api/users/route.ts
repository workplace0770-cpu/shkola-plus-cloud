import{env}from"cloudflare:workers";import{ensureAccountsTable,hashPassword,listSchoolUsers,makeUsername,temporaryPassword,userFromRequest,type SchoolRole}from"../../../db/accounts";
export async function GET(r:Request){await ensureAccountsTable();const a=await userFromRequest(r);if(a?.role!=="admin")return Response.json({error:"Нет доступа"},{status:403});return Response.json({users:await listSchoolUsers()})}
export async function POST(r:Request){await ensureAccountsTable();const a=await userFromRequest(r);if(a?.role!=="admin")return Response.json({error:"Нет доступа"},{status:403});const b=await r.json()as{fullName?:string;role?:SchoolRole;className?:string;subjectName?:string};const name=b.fullName?.trim(),className=b.className?.trim(),subjectName=b.subjectName?.trim();if(!name||!["student","teacher"].includes(b.role??"")||(b.role==="student"&&!className)||(b.role==="teacher"&&!subjectName))return Response.json({error:"Заполните имя, роль, класс или предмет"},{status:400});const duplicate=await env.DB.prepare("SELECT id FROM school_users WHERE lower(full_name)=lower(?) AND role=? AND COALESCE(class_name,'')=COALESCE(?, '')").bind(name,b.role,b.role==="student"?className:null).first();if(duplicate)return Response.json({error:"Такой пользователь уже существует"},{status:409});const max=await env.DB.prepare("SELECT COALESCE(MAX(id),0)+1 next FROM school_users").first<{next:number}>(),username=makeUsername(name,Number(max?.next??1)),password=temporaryPassword(),{salt,hash}=await hashPassword(password);await env.DB.prepare("INSERT INTO school_users(email,username,full_name,role,class_name,created_at,password_hash,password_salt,must_change_password)VALUES(NULL,?,?,?,?,?,?,?,1)").bind(username,name,b.role,b.role==="student"?className:null,new Date().toISOString(),hash,salt).run();await env.DB.batch([env.DB.prepare("CREATE TABLE IF NOT EXISTS school_classes(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL UNIQUE)"),env.DB.prepare("CREATE TABLE IF NOT EXISTS school_subjects(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL UNIQUE)")]);if(className)await env.DB.prepare("INSERT OR IGNORE INTO school_classes(name)VALUES(?)").bind(className).run();if(subjectName)await env.DB.prepare("INSERT OR IGNORE INTO school_subjects(name)VALUES(?)").bind(subjectName).run();return Response.json({users:await listSchoolUsers(),credentials:{username,password,fullName:name}},{status:201})}
export async function DELETE(r:Request){await ensureAccountsTable();const a=await userFromRequest(r);if(a?.role!=="admin")return Response.json({error:"Нет доступа"},{status:403});const id=Number(new URL(r.url).searchParams.get("id"));await env.DB.prepare("DELETE FROM school_users WHERE id=? AND role!='admin'").bind(id).run();return Response.json({users:await listSchoolUsers()})}
export async function PATCH(r:Request){await ensureAccountsTable();const a=await userFromRequest(r);if(a?.role!=="admin")return Response.json({error:"Нет доступа"},{status:403});const id=Number(new URL(r.url).searchParams.get("id")),u=await env.DB.prepare("SELECT id,username,full_name fullName,role FROM school_users WHERE id=? AND role!='admin'").bind(id).first<{id:number;username:string;fullName:string;role:string}>();if(!u)return Response.json({error:"Пользователь не найден"},{status:404});const password=temporaryPassword(),{salt,hash}=await hashPassword(password);await env.DB.batch([env.DB.prepare("UPDATE school_users SET password_hash=?,password_salt=?,must_change_password=1 WHERE id=?").bind(hash,salt,id),env.DB.prepare("DELETE FROM school_sessions WHERE user_id=?").bind(id)]);return Response.json({users:await listSchoolUsers(),credentials:{username:u.username,password,fullName:u.fullName}})}
